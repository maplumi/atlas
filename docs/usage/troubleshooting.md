# Troubleshooting

## Web viewer does not load
- Confirm `trunk serve` works.
- Confirm `wasm32-unknown-unknown` target is installed.

## Rendering looks different across GPUs
- Visual output may vary, but semantic results should remain deterministic.
