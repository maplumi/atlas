# Render Pipeline

Atlas renders in 2D and 3D from the same standardized world representation.

## Current
- Web viewer supports Canvas2D (2D) and WebGPU (3D).

## Planned
- core GPU crate owns device/context and render graph
- deterministic draw ordering
