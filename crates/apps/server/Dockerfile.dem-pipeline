# Deprecated: DEM pipeline container removed in favor of streaming terrain.
FROM debian:bookworm-slim
CMD ["/bin/sh", "-c", "echo 'DEM pipeline is deprecated. Use streaming terrain backend.' && exit 1"]
